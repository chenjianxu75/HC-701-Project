# Dataset Directory

This directory is ignored by Git and is expected to hold the local dataset copies only.

Use [`docs/DATASETS.md`](../docs/DATASETS.md) for:

- official download links
- mirror links
- expected folder layout
- conversion commands
